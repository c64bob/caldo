package view

templ DateScopedTasksPage(heading string, emptyMessage string, tasks []TaskRowView, create InlineTaskCreateView) {
	<section class="caldo-page">
		if len(tasks) == 0 {
			@EmptyState(emptyMessage, "Es gibt in dieser Ansicht gerade nichts zu bearbeiten.")
		} else {
			<ul class="caldo-list caldo-task-list" data-date-view-results>
				for _, task := range tasks {
					@TaskRow(task)
				}
			</ul>
		}
		@InlineTaskCreate(create)
	</section>
}
